# Enforce consistent installs across the team
engine-strict=true
# Prevent accidental publish
package-lock=true
# Avoid phantom dependency resolution issues on OneDrive/synced drives
cache=.npm-cache
